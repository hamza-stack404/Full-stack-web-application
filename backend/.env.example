# Database Configuration
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# Authentication Secret (generate a secure random string)
# Generate with: openssl rand -base64 48
BETTER_AUTH_SECRET=your-secret-key-here

# AI Chatbot Configuration (Choose ONE)
# Option 1: OpenRouter API (Recommended - supports multiple models)
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your-openrouter-api-key-here
OPENROUTER_MODEL=anthropic/claude-3.5-sonnet  # or any other model from OpenRouter

# Option 2: Google Gemini API with Automatic Key Rotation
# Get your API keys from: https://aistudio.google.com/app/apikey
#
# IMPORTANT: For automatic key rotation, use GEMINI_API_KEYS (plural) with comma-separated keys
# The system will automatically rotate to the next key when quota limits are reached
#
# Single key (basic setup):
# GEMINI_API_KEY=your-gemini-api-key-here
#
# Multiple keys with automatic rotation (recommended for free tier):
# GEMINI_API_KEYS=key1-here,key2-here,key3-here
#
# Example with 2 backup keys:
# GEMINI_API_KEYS=AIzaSyAbc123...,AIzaSyDef456...,AIzaSyGhi789...
#
# Security Notes:
# - Never commit actual API keys to git
# - Each key gets 20 requests/day on free tier
# - System automatically switches when quota is reached
# - Keys reset daily (24 hours)

# Frontend URL for CORS (Production)
FRONTEND_URL=http://localhost:3000

# Rate Limiting (Optional)
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60

# Debug Mode (Optional, DO NOT use in production)
DEBUG=false
